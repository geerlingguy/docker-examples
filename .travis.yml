---
sudo: required

env:
  matrix:
    - TEST_DIR: "flask"
      START_COMMAND: "docker-compose up -d"
      STOP_COMMAND: "docker-compose stop"

services:
  - docker

script:
  - cd $TEST_DIR && $START_COMMAND
  - cd $TEST_DIR && $STOP_COMMAND
